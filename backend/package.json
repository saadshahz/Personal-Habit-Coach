{
    "name": "personal-habit-coach-backend",
    "version": "1.0.0",
    "description": "Backend for Personal Habit Coach",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "build": "cd ../frontend && npm install && npm run build",
        "postinstall": "npm run build"
    },
    "engines": {
        "node": ">=14.0.0",
        "npm": ">=6.0.0"
    },
    "dependencies": {
        "@google/generative-ai": "^0.24.1",
        "body-parser": "^1.20.2",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    }
}